# Dependencies
node_modules/

# clasp auth token (contains your Google OAuth token — never commit)
.clasprc.json
~/.clasprc.json

# Secrets & environment
.env
.env.*
secrets.json
config.local.js

# Apps Script — clasp pulls these; don't commit pulled copies
gas/*.js.map

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# Local dev configs (never commit)
local/.dev-config.json
local/app-credentials.json
